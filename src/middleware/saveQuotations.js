

const saveQuo = async(uid = String, bus = String, dato = {} ) => { 
    
    const id = uid;
    const bs = bus;
    const dat = dato;

    console.log(id);
    // console.log(bs);
    // console.log(dat);

};